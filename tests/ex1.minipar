let a: i32 = 10
let b: bool = true

# comentário simples
/* comentario composto
 * multilinha
*/
func soma(num1: i32, num2: i32) -> i32
{
    let s: i32 = num1 + num2
    while(a < 20){
      a = a + 1
      print(a)
      if(a == 15){break}
    }

    return s + 10
}

func main()
{
    print(soma(2, 3))
}
